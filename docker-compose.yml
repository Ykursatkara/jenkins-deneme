version: '3.8'

services:
    jenkins_service:
        build: .
        container_name: jenkins_deneme
        restart: always
        ports:
            - "1004:1004"
        environment:
          TZ: "Europe/Istanbul"
